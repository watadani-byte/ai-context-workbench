/// Shared implementation boundary for AI Context Workbench.
///
/// Stage 0 intentionally contains no document, preview, validation, or
/// clipboard behavior. Those components are introduced only in their
/// authorized implementation stages.
public enum WorkbenchCore {
    public static let productName = "AI Context Workbench"
    public static let version = "0.1"
}
